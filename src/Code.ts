/**
 * GAS entrypoint placeholder.
 *
 * The actual ticket application workflow will be implemented in later steps.
 * Keep this file free of real form IDs, spreadsheet IDs, member data, and
 * LivePocket serial codes.
 */
function healthCheck(): string {
  return "ticket-application-gas:ok";
}

